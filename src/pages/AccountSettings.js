import React from "react";
import styled from "styled-components";

const AccountSettings = () => {
  return <Wrapper className="page-100"></Wrapper>;
};

const Wrapper = styled.section``;

export default AccountSettings;
